# lakesideanalytics.io

Personal portfolio for Sachin Seth — a single static `index.html`, served via GitHub Pages at https://lakesideanalytics.io.

## Editing

Edit `index.html` and push to `main`. GitHub Pages redeploys automatically.

The `CNAME` file binds this repo to the custom domain `lakesideanalytics.io` — leave it in place.
